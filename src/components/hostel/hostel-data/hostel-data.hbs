<div class="hostel__card">
    <button class="hostel__button-round" id="wishlist-button">kek</button>
    <p class="hostel__title hostel__title--orange">{{name}}</p>
    <p class="hostel__star hostel__title hostel__title--orange">
        {{#if rating}}
        Рейтинг: {{rating}}
        {{else}}
        Поставьте оценку первым!
        {{/if}}
    </p>
    <p class="hostel__title hostel__text--blue">Количество комментариев: {{countComments}}</p>
    <p class="hostel__title hostel__title--orange">{{location}}</p>
    <button class="hostel__button-round" id="map-button">Показать на карте</button>
    <p class="hostel__text hostel__text--blue">
        {{description}}
    </p>
</div>
